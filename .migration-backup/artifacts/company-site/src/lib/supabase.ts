// Supabase has been replaced with Replit's built-in PostgreSQL.
// All data is fetched via the API server (/api/*) using the api-client-react hooks.
// This stub prevents import errors in any file that still references this module.
export const supabase = null;
